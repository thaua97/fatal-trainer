import { describe, it, expect } from 'vitest'
import FTFilterPanel from './FTFilterPanel.vue'

describe('FTFilterPanel', () => {
  it('is defined', () => {
    expect(FTFilterPanel).toBeDefined()
  })
})
