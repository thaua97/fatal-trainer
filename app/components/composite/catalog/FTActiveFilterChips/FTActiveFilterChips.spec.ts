import { describe, it, expect } from 'vitest'
import FTActiveFilterChips from './FTActiveFilterChips.vue'

describe('FTActiveFilterChips', () => {
  it('is defined', () => {
    expect(FTActiveFilterChips).toBeDefined()
  })
})
