import { describe, it, expect } from 'vitest'
import FTCatalogToolbar from './FTCatalogToolbar.vue'

describe('FTCatalogToolbar', () => {
  it('is defined', () => {
    expect(FTCatalogToolbar).toBeDefined()
  })
})
