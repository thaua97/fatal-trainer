import { describe, it, expect } from 'vitest'
import FTProfileHero from './FTProfileHero.vue'

describe('FTProfileHero', () => {
  it('is defined', () => {
    expect(FTProfileHero).toBeDefined()
  })
})
