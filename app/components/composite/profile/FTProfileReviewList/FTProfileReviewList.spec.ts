import { describe, it, expect } from 'vitest'
import FTProfileReviewList from './FTProfileReviewList.vue'

describe('FTProfileReviewList', () => {
  it('is defined', () => {
    expect(FTProfileReviewList).toBeDefined()
  })
})
