import { describe, it, expect } from 'vitest'
import FTProfileHeader from './FTProfileHeader.vue'

describe('FTProfileHeader', () => {
  it('is defined', () => {
    expect(FTProfileHeader).toBeDefined()
  })
})
