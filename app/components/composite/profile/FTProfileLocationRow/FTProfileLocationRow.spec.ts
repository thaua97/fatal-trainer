import { describe, it, expect } from 'vitest'
import FTProfileLocationRow from './FTProfileLocationRow.vue'

describe('FTProfileLocationRow', () => {
  it('is defined', () => {
    expect(FTProfileLocationRow).toBeDefined()
  })
})
