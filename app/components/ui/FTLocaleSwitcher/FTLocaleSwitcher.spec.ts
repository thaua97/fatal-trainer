import { describe, it, expect } from 'vitest'
import { mountFT } from '@tests/helpers/mount-ft'
import FTLocaleSwitcher from './FTLocaleSwitcher.vue'

describe('FTLocaleSwitcher', () => {
  it('is defined', () => {
    expect(FTLocaleSwitcher).toBeDefined()
  })
})
